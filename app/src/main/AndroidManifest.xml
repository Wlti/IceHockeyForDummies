<?xml version="1.0" encoding="UTF-8"?>

    <manifest package="com.example.icehockeyfordummies" xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:theme="@style/Theme.AppCompat.Light.DarkActionBar" android:supportsRtl="true" android:roundIcon="@mipmap/ic_launcher_round" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:allowBackup="true">
        <activity android:name=".user_interface.MainActivity">


    <intent-filter>

    <action android:name="android.intent.action.MAIN"/>

    <category android:name="android.intent.category.LAUNCHER"/>

</intent-filter>

</activity>

    <activity android:name=".user_interface.AboutActivity"/>

    <activity android:name=".user_interface.ClubActivity"/>

    <activity android:name=".user_interface.FirebaseActivity"/>

    <activity android:name=".user_interface.LeagueActivity"/>

    <activity android:name=".user_interface.PlayerProfileActivity"/>

    <activity android:name=".user_interface.SettingsActivity"/>
        <activity android:name=".user_interface.FirebaseDeleteDB"/>

    <activity android:name=".user_interface.TeamActivity"> </activity>

</application>

</manifest>